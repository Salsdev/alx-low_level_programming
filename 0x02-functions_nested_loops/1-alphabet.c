#include "main.h"

/**
 * main - starting point
 *
 * Description: Alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (char i = 'a'; i < 'z'; i++)
	{
		putchar("%c\n", char);
	}
	return (0);
}
